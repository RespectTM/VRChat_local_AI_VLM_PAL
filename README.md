VRChat Local AI — Fresh Reinitialized Scaffold

This workspace has been reinitialized to a minimal scaffold per your request.

Quick start:

- Create a venv: `python -m venv .venv`
- Activate (PowerShell): `.\.venv\Scripts\Activate.ps1`
- Install deps: `pip install -r requirements.txt`
- Run: `python main.py`

If you want the previous project restored, provide a Git remote URL or a backup.
